This folder contains the following files:

| Name         | Description |
| ------------ | ----------- |
| `menu.yml`   | This lists the items found on the main menu and the home page. Each group of items (e.g., 'theme') contains a title (e.g., 'theme_title') and descriptive text (e.g., 'theme_text'). The items within a group (e.g., 'theme_list') have a name ('title'), an image ('icon'—specifically a font code point), and a link to a web page. |
| `footer.yml` | This defines the footer for each page, featuring a short description of MHHRI in 'about_text' and a list of items in 'social_list'. The social list references social media platforms using a 'title', 'icon', and 'link'. |
| `README.md`  | (this file) |
